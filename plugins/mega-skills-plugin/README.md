# mega-skills-plugin

Stress-test plugin containing 220 synthetic skills used to exercise the
marketplace parser at scale. All skills are static markdown — they do not
import or run any code.

Generated for `test-errant-marketplace` to verify the parser handles a
plugin with many components without breaking the marketplace shell or
skipping the `errant-plugin` entry.
